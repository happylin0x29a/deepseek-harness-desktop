{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the local splash page: core events and window APIs, plus the startup-status poll it renders. The engine's own page is a remote origin and is covered by the `engine-surface` capability instead.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-startup-status"
  ]
}
